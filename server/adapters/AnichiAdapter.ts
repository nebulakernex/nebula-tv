import { fetchJson } from '../../src/lib/api';
// Will implement Anichi logic here next
export const AnichiAdapter = {
  getHome: async () => [],
  search: async (query: string) => [],
  getDetails: async (id: string) => null,
  getEpisodes: async (id: string) => [],
  resolveSources: async (id: string) => []
};
